import ScheduleForm from '@/app/schedule/ScheduleForm';

export const metadata = {
  title: 'Schedule an Appointment | Domestic and Foreign Auto Body Inc.',
  description: 'Schedule your auto body repair estimate with Cleveland\'s trusted experts. Upload photos of damage for faster service.',
};

export default function SchedulePage() {
  return <ScheduleForm />;
}
